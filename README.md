Upon downloading, run
"xattr -cr ~/Downloads/GhostLocation.app" in terminal to appropriately run the zip.

run "sudo python3 -m pymobiledevice3 remote tunneld" in terminal to obtain YOUR "fd44" and "00000" --rsd
